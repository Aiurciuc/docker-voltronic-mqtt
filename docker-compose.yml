name: voltronic-mqtt-custom
services:
    python:
        device_cgroup_rules:
            - c 188:* rmw
        stdin_open: true
        tty: true
        container_name: voltronic-mqtt-custom

volumes:
  pgdata: {}